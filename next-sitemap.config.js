/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://outlook-logistics.ru',
    generateRobotsTxt: true, // (optional)
    // ...other options
}